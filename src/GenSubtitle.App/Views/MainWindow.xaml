<Window x:Class="GenSubtitle.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:GenSubtitle.App.Views"
        xmlns:cmd="clr-namespace:GenSubtitle.App"
        Title="GenSubtitle" Height="820" Width="1280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{StaticResource VsBackgroundBrush}">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="32" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" />
  </shell:WindowChrome.WindowChrome>

  <Window.CommandBindings>
    <CommandBinding Command="{x:Static cmd:AppCommands.ExportTaskCommand}" Executed="OnExportTaskCommand" CanExecute="OnExportTaskCanExecute" />
    <CommandBinding Command="{x:Static cmd:AppCommands.DeleteTaskCommand}" Executed="OnDeleteTaskCommand" CanExecute="OnDeleteTaskCanExecute" />
  </Window.CommandBindings>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="180" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="{StaticResource VsHeaderBrush}" BorderBrush="{StaticResource VsBorderBrush}" BorderThickness="0,0,0,1" Padding="8,0" MouseLeftButtonDown="OnTitleBarMouseDown">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" VerticalAlignment="Center">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="GenSubtitle" FontSize="16" FontWeight="SemiBold" Margin="0,0,12,0" VerticalAlignment="Center" />
          <Menu Grid.Column="1" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <MenuItem Header="File">
              <MenuItem Header="Import Files" Click="OnImportFiles" />
              <Separator />
              <MenuItem Header="Exit" Click="OnExit" />
            </MenuItem>
            <MenuItem Header="Edit">
              <MenuItem Header="Undo" IsEnabled="False" />
              <MenuItem Header="Redo" IsEnabled="False" />
            </MenuItem>
            <MenuItem Header="View">
              <MenuItem Header="Toggle Queue Panel" Click="OnToggleQueue" />
              <MenuItem Header="Toggle Preview" Click="OnTogglePreview" />
              <MenuItem Header="Compact Mode" Click="OnToggleCompact" />
            </MenuItem>
            <MenuItem Header="Tools">
              <MenuItem Header="Settings" Click="OnOpenSettings" />
              <MenuItem Header="Run Diagnostics" Click="OnRunDiagnostics" />
            </MenuItem>
            <MenuItem Header="Help">
              <MenuItem Header="About" Click="OnAbout" />
              <MenuItem Header="Open Log Folder" Click="OnOpenLogFolder" />
            </MenuItem>
          </Menu>
          <TextBlock Grid.Column="2" Text="{Binding SelectedVideoPath}" TextTrimming="CharacterEllipsis" Foreground="{StaticResource VsMutedTextBrush}" Width="220" Margin="12,0,0,0" VerticalAlignment="Center" />
        </Grid>

        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Style="{StaticResource ToolbarButton}" Click="OnImportFiles" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <StackPanel Orientation="Horizontal">
              <TextBlock Style="{StaticResource IconGlyph}" Text="&#xE118;" />
              <TextBlock Text="Import" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource ToolbarButton}" Click="OnOpenSettings" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <StackPanel Orientation="Horizontal">
              <TextBlock Style="{StaticResource IconGlyph}" Text="&#xE713;" />
              <TextBlock Text="Settings" />
            </StackPanel>
          </Button>

          <Button Style="{StaticResource WindowButton}" Click="OnMinimize" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <TextBlock Style="{StaticResource WindowGlyph}" Text="&#xE921;" />
          </Button>
          <Button Style="{StaticResource WindowButton}" Click="OnMaximize" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <TextBlock Style="{StaticResource WindowGlyph}" Text="&#xE922;" />
          </Button>
          <Button Style="{StaticResource WindowButton}" Click="OnClose" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <TextBlock Style="{StaticResource WindowGlyph}" Text="&#xE8BB;" />
          </Button>
        </StackPanel>
      </Grid>
    </Border>

    <Grid Grid.Row="1" Margin="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition x:Name="QueueColumn" Width="300" />
        <ColumnDefinition x:Name="QueueGapColumn" Width="12" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border x:Name="QueuePanel" Grid.Column="0" Style="{StaticResource Card}" Padding="10">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBlock Text="Task Queue" Style="{StaticResource SectionHeader}" />
          <ListBox Grid.Row="1" ItemsSource="{Binding Queue.Tasks}" SelectedItem="{Binding SelectedTask}" Margin="0,6,0,0" SelectionChanged="OnTaskSelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Border BorderBrush="{StaticResource VsBorderBrush}" BorderThickness="0,0,0,1" Padding="8">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="{Binding FileName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTipService.InitialShowDelay="1000">
                      <TextBlock.ToolTip>
                        <ToolTip Content="{Binding FileName}" />
                      </TextBlock.ToolTip>
                    </TextBlock>
                    <TextBlock Grid.Row="1" Text="{Binding StatusText}" Foreground="{StaticResource VsMutedTextBrush}" Margin="0,2,0,2">
                      <TextBlock.Style>
                        <Style TargetType="TextBlock">
                          <Setter Property="Visibility" Value="Collapsed" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding ProgressText}" Value="{x:Null}">
                              <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ProgressText}" Value="{x:Static sys:String.Empty}">
                              <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </TextBlock.Style>
                    </TextBlock>
                    <TextBox Grid.Row="2" Text="{Binding ErrorMessage, Mode=OneWay}" Foreground="#C62828" Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Margin="0,0,0,4">
                      <TextBox.Style>
                        <Style TargetType="TextBox">
                          <Setter Property="Visibility" Value="Visible" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                              <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Static sys:String.Empty}">
                              <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </TextBox.Style>
                    </TextBox>
                    <ProgressBar Grid.Row="3" Value="{Binding Progress, Mode=OneWay}" Maximum="100" Height="6" Margin="0,2,0,0" HorizontalAlignment="Stretch" />
                    <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,6,0,0">
                      <ProgressBar Width="18" Height="18" IsIndeterminate="True" Margin="0,0,6,0">
                        <ProgressBar.Style>
                          <Style TargetType="ProgressBar">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding IsTranslating}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </ProgressBar.Style>
                      </ProgressBar>
                      <TextBlock Text="{Binding ProgressText}" Foreground="{StaticResource VsMutedTextBrush}" Margin="0,0,8,0" />
                      <TextBlock Text="{Binding TranslationEtaText}" Foreground="{StaticResource VsMutedTextBrush}" />
                    </StackPanel>
                  </Grid>
                </Border>
              </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
              <Style TargetType="ListBoxItem">
                <Setter Property="ContextMenu">
                  <Setter.Value>
                    <ContextMenu>
                      <MenuItem Header="Pause" Command="{Binding PauseCommand}" />
                      <MenuItem Header="Resume" Command="{Binding ResumeCommand}" />
                      <MenuItem Header="Cancel" Command="{Binding CancelCommand}" />
                      <MenuItem Header="Open Folder" Command="{Binding OpenFolderCommand}" />
                      <MenuItem Header="Delete" Command="{x:Static cmd:AppCommands.DeleteTaskCommand}" CommandParameter="{Binding}" />
                      <Separator />
                      <MenuItem Header="Export" Command="{x:Static cmd:AppCommands.ExportTaskCommand}" CommandParameter="{Binding}" />
                    </ContextMenu>
                  </Setter.Value>
                </Setter>
              </Style>
            </ListBox.ItemContainerStyle>
          </ListBox>
        </Grid>
      </Border>

      <Grid Grid.Column="2" DataContext="{Binding SelectedTask}">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8" VerticalAlignment="Center">
          <TextBlock Text="Detected Language" Style="{StaticResource SectionHeader}" Margin="0,0,8,0" />
          <TextBlock Text="{Binding DetectedLanguage}" FontWeight="SemiBold" />
        </StackPanel>

        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="PreviewColumn" Width="2*" />
            <ColumnDefinition x:Name="PreviewGapColumn" Width="12" />
            <ColumnDefinition Width="3*" />
          </Grid.ColumnDefinitions>

          <Grid x:Name="PreviewPanel" Grid.Column="0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Preview" Style="{StaticResource SectionHeader}" />
            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,10">
              <Border Background="#0E0E0E" CornerRadius="4">
                <MediaElement x:Name="Player" LoadedBehavior="Manual" UnloadedBehavior="Manual" ScrubbingEnabled="True" />
              </Border>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
              <Button Content="Play" Width="80" Margin="0,0,8,0" Click="OnPlay" />
              <Button Content="Pause" Width="80" Margin="0,0,8,0" Click="OnPause" />
              <Button Content="Align Start" Width="100" Margin="0,0,8,0" Click="OnAlignStart" />
              <Button Content="Align End" Width="100" Click="OnAlignEnd" />
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Vertical">
              <Slider x:Name="TimelineSlider" Minimum="0" Maximum="1" ValueChanged="OnTimelineChanged" PreviewMouseLeftButtonDown="OnSeekStart" PreviewMouseLeftButtonUp="OnSeekEnd" PreviewMouseMove="OnSeekMove" />
              <TextBlock x:Name="TimeDisplay" Text="00:00 / 00:00" Margin="0,4,0,0" Foreground="{StaticResource VsMutedTextBrush}" />
            </StackPanel>
          </Grid>

          <Border Grid.Column="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding Segments}" SelectedItem="{Binding SelectedSegment}" AutoGenerateColumns="False" CanUserAddRows="False">
              <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="40" />
                <DataGridTextColumn Header="Start" Binding="{Binding Start}" Width="90" />
                <DataGridTextColumn Header="End" Binding="{Binding End}" Width="90" />
                <DataGridTextColumn Header="Source" Binding="{Binding SourceText}" Width="*" />
                <DataGridTextColumn Header="中文" Binding="{Binding ZhText}" Width="*" />
              </DataGrid.Columns>
            </DataGrid>
          </Border>
        </Grid>
      </Grid>
    </Grid>

    <Border Grid.Row="2" Background="{StaticResource VsHeaderBrush}" BorderBrush="{StaticResource VsBorderBrush}" BorderThickness="1,1,1,0" Padding="8">
      <DockPanel>
        <TextBlock Text="{Binding Queue.QueueSummary}" DockPanel.Dock="Left" Foreground="{StaticResource VsMutedTextBrush}" />
        <TextBlock Text="Auto-Translate:" Margin="16,0,6,0" DockPanel.Dock="Right" Foreground="{StaticResource VsMutedTextBrush}" />
        <TextBlock Text="{Binding Settings.AutoTranslateEnabled}" DockPanel.Dock="Right" />
      </DockPanel>
    </Border>

    <Border Grid.Row="3" Background="{StaticResource VsSurfaceBrush}" BorderBrush="{StaticResource VsBorderBrush}" BorderThickness="1,1,1,0" Padding="8">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
          <TextBlock Text="Console" Style="{StaticResource SectionHeader}" DockPanel.Dock="Left" />
          <Button Content="Clear" Style="{StaticResource SmallButton}" DockPanel.Dock="Right" Click="OnClearLog" />
        </DockPanel>
        <TextBox x:Name="LogBox" Grid.Row="1" Text="{Binding Queue.LogText}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="{StaticResource VsPanelBrush}" BorderThickness="1" TextChanged="OnLogTextChanged" Loaded="OnLogLoaded" />
      </Grid>
    </Border>
  </Grid>
</Window>
