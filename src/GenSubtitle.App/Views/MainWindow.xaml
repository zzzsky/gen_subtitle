<Window x:Class="GenSubtitle.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:GenSubtitle.App.Views"
        xmlns:cmd="clr-namespace:GenSubtitle.App"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="GenSubtitle" Height="820" Width="1280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource MaterialDesignPaper}">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="32" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" />
  </shell:WindowChrome.WindowChrome>

  <Window.CommandBindings>
    <CommandBinding Command="{x:Static cmd:AppCommands.ExportTaskCommand}" Executed="OnExportTaskCommand" CanExecute="OnExportTaskCanExecute" />
    <CommandBinding Command="{x:Static cmd:AppCommands.DeleteTaskCommand}" Executed="OnDeleteTaskCommand" CanExecute="OnDeleteTaskCanExecute" />
  </Window.CommandBindings>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="180" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="{DynamicResource PrimaryHueLightBrush}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="8,0" MouseLeftButtonDown="OnTitleBarMouseDown">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" VerticalAlignment="Center">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="GenSubtitle" FontSize="16" FontWeight="SemiBold" Margin="0,0,12,0" VerticalAlignment="Center" />
          <Menu Grid.Column="1" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <MenuItem Header="File">
              <MenuItem Header="Import Files" Click="OnImportFiles" />
              <Separator />
              <MenuItem Header="Exit" Click="OnExit" />
            </MenuItem>
            <MenuItem Header="Edit">
              <MenuItem Header="Undo" IsEnabled="False" />
              <MenuItem Header="Redo" IsEnabled="False" />
            </MenuItem>
            <MenuItem Header="View">
              <MenuItem Header="Toggle Queue Panel" Click="OnToggleQueue" />
              <MenuItem Header="Toggle Preview" Click="OnTogglePreview" />
              <MenuItem Header="Compact Mode" Click="OnToggleCompact" />
            </MenuItem>
            <MenuItem Header="Tools">
              <MenuItem Header="Settings" Click="OnOpenSettings" />
              <MenuItem Header="Run Diagnostics" Click="OnRunDiagnostics" />
            </MenuItem>
            <MenuItem Header="Help">
              <MenuItem Header="About" Click="OnAbout" />
              <MenuItem Header="Open Log Folder" Click="OnOpenLogFolder" />
            </MenuItem>
          </Menu>
          <TextBlock Grid.Column="2" Text="{Binding SelectedVideoPath}" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource MaterialDesignBodyLight}" Width="220" Margin="12,0,0,0" VerticalAlignment="Center" />
        </Grid>

        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Style="{StaticResource MaterialDesignRaisedButton}" Click="OnImportFiles" shell:WindowChrome.IsHitTestVisibleInChrome="True" Margin="0,0,6,0">
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="FilePlus" Width="18" Height="18" Margin="0,0,6,0" VerticalAlignment="Center" />
              <TextBlock Text="Import" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource MaterialDesignRaisedButton}" Click="OnOpenSettings" shell:WindowChrome.IsHitTestVisibleInChrome="True" Margin="0,0,6,0">
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="Cog" Width="18" Height="18" Margin="0,0,6,0" VerticalAlignment="Center" />
              <TextBlock Text="Settings" />
            </StackPanel>
          </Button>

          <Button Style="{StaticResource MaterialDesignFlatButton}" Click="OnMinimize" shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="40" Height="30" Padding="0">
            <materialDesign:PackIcon Kind="WindowMinimize" Width="16" Height="16" />
          </Button>
          <Button Style="{StaticResource MaterialDesignFlatButton}" Click="OnMaximize" shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="40" Height="30" Padding="0">
            <materialDesign:PackIcon Kind="WindowMaximize" Width="16" Height="16" />
          </Button>
          <Button Style="{StaticResource MaterialDesignFlatButton}" Click="OnClose" shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="40" Height="30" Padding="0">
            <materialDesign:PackIcon Kind="Close" Width="16" Height="16" />
          </Button>
        </StackPanel>
      </Grid>
    </Border>

    <Grid Grid.Row="1" Margin="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition x:Name="QueueColumn" Width="300" />
        <ColumnDefinition x:Name="QueueGapColumn" Width="12" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <materialDesign:Card x:Name="QueuePanel" Grid.Column="0" Padding="10" Margin="0,0,0,0">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBlock Text="Task Queue" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" />
          <ListBox Grid.Row="1" Style="{StaticResource MaterialDesignListBox}" ItemsSource="{Binding Queue.Tasks}" SelectedItem="{Binding SelectedTask}" Margin="0,6,0,0" SelectionChanged="OnTaskSelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <materialDesign:Card Padding="8" Margin="0,4,0,4">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="{Binding FileName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTipService.InitialShowDelay="1000">
                      <TextBlock.ToolTip>
                        <ToolTip Content="{Binding FileName}" />
                      </TextBlock.ToolTip>
                    </TextBlock>
                    <TextBlock Grid.Row="1" Text="{Binding StatusText}" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,2,0,2">
                      <TextBlock.Style>
                        <Style TargetType="TextBlock">
                          <Setter Property="Visibility" Value="Collapsed" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding ProgressText}" Value="{x:Null}">
                              <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ProgressText}" Value="{x:Static sys:String.Empty}">
                              <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </TextBlock.Style>
                    </TextBlock>
                    <TextBox Grid.Row="2" Text="{Binding ErrorMessage, Mode=OneWay}" Foreground="#C62828" Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" Margin="0,0,0,4">
                      <TextBox.Style>
                        <Style TargetType="TextBox">
                          <Setter Property="Visibility" Value="Visible" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                              <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Static sys:String.Empty}">
                              <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </TextBox.Style>
                    </TextBox>
                    <ProgressBar Grid.Row="3" Value="{Binding Progress, Mode=OneWay}" Maximum="100" Height="6" Margin="0,2,0,0" HorizontalAlignment="Stretch" />
                    <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,6,0,0">
                      <ProgressBar Width="18" Height="18" IsIndeterminate="True" Margin="0,0,6,0">
                        <ProgressBar.Style>
                          <Style TargetType="ProgressBar">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding IsTranslating}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </ProgressBar.Style>
                      </ProgressBar>
                      <TextBlock Text="{Binding ProgressText}" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,0,8,0" />
                      <TextBlock Text="{Binding TranslationEtaText}" Foreground="{DynamicResource MaterialDesignBodyLight}" />
                    </StackPanel>
                  </Grid>
                </materialDesign:Card>
              </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
              <Style TargetType="ListBoxItem">
                <Setter Property="ContextMenu">
                  <Setter.Value>
                    <ContextMenu>
                      <MenuItem Header="Pause" Command="{Binding PauseCommand}" />
                      <MenuItem Header="Resume" Command="{Binding ResumeCommand}" />
                      <MenuItem Header="Cancel" Command="{Binding CancelCommand}" />
                      <MenuItem Header="Open Folder" Command="{Binding OpenFolderCommand}" />
                      <MenuItem Header="Delete" Command="{x:Static cmd:AppCommands.DeleteTaskCommand}" CommandParameter="{Binding}" />
                      <Separator />
                      <MenuItem Header="Export" Command="{x:Static cmd:AppCommands.ExportTaskCommand}" CommandParameter="{Binding}" />
                    </ContextMenu>
                  </Setter.Value>
                </Setter>
              </Style>
            </ListBox.ItemContainerStyle>
          </ListBox>
        </Grid>
      </materialDesign:Card>

      <Grid Grid.Column="2" DataContext="{Binding SelectedTask}">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8" VerticalAlignment="Center">
          <TextBlock Text="Detected Language" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Margin="0,0,8,0" />
          <TextBlock Text="{Binding DetectedLanguage}" FontWeight="SemiBold" />
        </StackPanel>

        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="PreviewColumn" Width="2.2*" />
            <ColumnDefinition x:Name="PreviewGapColumn" Width="12" />
            <ColumnDefinition Width="2.8*" />
          </Grid.ColumnDefinitions>

          <Grid x:Name="PreviewPanel" Grid.Column="0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Preview" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" />
            <materialDesign:Card Grid.Row="1" Margin="0,0,0,10">
              <Border Background="#0E0E0E" CornerRadius="4">
                <MediaElement x:Name="Player" LoadedBehavior="Manual" UnloadedBehavior="Manual" ScrubbingEnabled="True" />
              </Border>
            </materialDesign:Card>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
              <Button Content="Play" Style="{StaticResource MaterialDesignRaisedButton}" Width="80" Margin="0,0,8,0" Click="OnPlay" />
              <Button Content="Pause" Style="{StaticResource MaterialDesignRaisedButton}" Width="80" Margin="0,0,8,0" Click="OnPause" />
              <Button Content="Align Start" Style="{StaticResource MaterialDesignOutlinedButton}" Width="110" Margin="0,0,8,0" Click="OnAlignStart" />
              <Button Content="Align End" Style="{StaticResource MaterialDesignOutlinedButton}" Width="110" Click="OnAlignEnd" />
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Vertical">
              <Slider x:Name="TimelineSlider" Style="{StaticResource MaterialDesignSlider}" Minimum="0" Maximum="1" ValueChanged="OnTimelineChanged" PreviewMouseLeftButtonDown="OnSeekStart" PreviewMouseLeftButtonUp="OnSeekEnd" PreviewMouseMove="OnSeekMove" />
              <TextBlock x:Name="TimeDisplay" Text="00:00 / 00:00" Margin="0,4,0,0" Foreground="{DynamicResource MaterialDesignBodyLight}" />
            </StackPanel>
          </Grid>

          <materialDesign:Card Grid.Column="2">
            <DataGrid Style="{StaticResource MaterialDesignDataGrid}" ItemsSource="{Binding Segments}" SelectedItem="{Binding SelectedSegment}" AutoGenerateColumns="False" CanUserAddRows="False" materialDesign:DataGridAssist.CellPadding="8">
              <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="40" />
                <DataGridTextColumn Header="Start" Binding="{Binding Start}" Width="90" />
                <DataGridTextColumn Header="End" Binding="{Binding End}" Width="90" />
                <DataGridTextColumn Header="Source" Binding="{Binding SourceText}" Width="*" />
                <DataGridTextColumn Header="中文" Binding="{Binding ZhText}" Width="*" />
              </DataGrid.Columns>
            </DataGrid>
          </materialDesign:Card>
        </Grid>
      </Grid>
    </Grid>

    <Border Grid.Row="2" Background="{DynamicResource PrimaryHueLightBrush}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1,1,1,0" Padding="8">
      <DockPanel>
        <TextBlock Text="{Binding Queue.QueueSummary}" DockPanel.Dock="Left" Foreground="{DynamicResource MaterialDesignBodyLight}" />
        <TextBlock Text="Auto-Translate:" Margin="16,0,6,0" DockPanel.Dock="Right" Foreground="{DynamicResource MaterialDesignBodyLight}" />
        <TextBlock Text="{Binding Settings.AutoTranslateEnabled}" DockPanel.Dock="Right" />
      </DockPanel>
    </Border>

    <materialDesign:Card Grid.Row="3" Padding="8" Margin="0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
          <TextBlock Text="Console" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" DockPanel.Dock="Left" />
          <Button Content="Clear" Style="{StaticResource MaterialDesignOutlinedButton}" DockPanel.Dock="Right" Click="OnClearLog" />
        </DockPanel>
        <TextBox x:Name="LogBox" Grid.Row="1" Style="{StaticResource MaterialDesignOutlinedTextBox}" Text="{Binding Queue.LogText}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" TextChanged="OnLogTextChanged" Loaded="OnLogLoaded" />
      </Grid>
    </materialDesign:Card>
  </Grid>
</Window>
