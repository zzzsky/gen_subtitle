<Window x:Class="GenSubtitle.App.Views.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Export" Height="320" Width="360" WindowStartupLocation="CenterOwner">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" Margin="0,0,0,12" materialDesign:ElevationAssist.Elevation="Dp4">
      <DockPanel>
        <TextBlock Text="Export" Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
        <TextBlock Text="  Video options" Style="{StaticResource MaterialDesignBody1TextBlock}" Foreground="{DynamicResource MaterialDesignBodyLight}" />
      </DockPanel>
    </materialDesign:ColorZone>

    <materialDesign:Card Grid.Row="1" Margin="0,0,0,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
          <TextBlock Text="Format" VerticalAlignment="Center" Width="80" Foreground="{DynamicResource MaterialDesignBodyLight}" />
          <ComboBox x:Name="FormatBox" Width="200" Style="{StaticResource MaterialDesignOutlinedComboBox}">
            <ComboBoxItem Content="SRT (SubRip)" Tag="Srt" />
            <ComboBoxItem Content="VTT (WebVTT)" Tag="Vtt" />
            <ComboBoxItem Content="ASS (Advanced SubStation Alpha)" Tag="Ass" />
            <ComboBoxItem Content="TXT (Plain Text)" Tag="Txt" />
            <ComboBoxItem Content="Bilingual Table" Tag="Bilingual" />
          </ComboBox>
        </StackPanel>

        <RadioButton x:Name="SoftMuxRadio" Content="Soft Mux" GroupName="ExportMode" Grid.Row="1" Margin="0,0,0,8" Style="{StaticResource MaterialDesignRadioButton}" />
        <RadioButton x:Name="BurnInRadio" Content="Burn-in (Hard Subs)" GroupName="ExportMode" Grid.Row="2" Margin="0,0,0,8" Style="{StaticResource MaterialDesignRadioButton}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,12">
          <TextBlock Text="ASS Style" VerticalAlignment="Center" Width="80" Foreground="{DynamicResource MaterialDesignBodyLight}" />
          <TextBox x:Name="StyleBox" Width="200" Style="{StaticResource MaterialDesignOutlinedTextBox}" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Width="90" Margin="0,0,8,0" Click="OnCancel" Style="{StaticResource MaterialDesignOutlinedButton}">Cancel</Button>
          <Button Width="90" Click="OnSave" Style="{StaticResource MaterialDesignRaisedButton}">Export</Button>
        </StackPanel>
      </Grid>
    </materialDesign:Card>
  </Grid>
</Window>
