<Window x:Class="GenSubtitle.App.Views.ConfirmDeleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete Task" Height="170" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource VsBackgroundBrush}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Delete completed task?" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8" />
    <CheckBox x:Name="DeleteFilesCheckBox" Grid.Row="1" Content="Also delete local files" IsChecked="False" Margin="0,0,0,12" />
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="OnCancel" />
      <Button Content="Delete" Width="90" Click="OnDelete" />
    </StackPanel>
  </Grid>
</Window>
