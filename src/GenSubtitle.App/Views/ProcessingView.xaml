<UserControl x:Class="GenSubtitle.App.Views.ProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:GenSubtitle.App.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance vm:ProcessingViewModel}"
             mc:Ignorable="d">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">
                <TextBlock Text="任务列表" FontSize="24" FontWeight="Bold" Margin="0,0,0,16"/>

                <!-- Placeholder for task list - will be implemented in Task 13 -->
                <TextBlock Text="任务列表将在这里显示" FontSize="14" Foreground="{DynamicResource MaterialDesignBodyLightBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Padding="32"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
