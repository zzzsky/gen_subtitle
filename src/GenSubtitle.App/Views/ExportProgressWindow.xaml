<Window x:Class="GenSubtitle.App.Views.ExportProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Exporting" Height="220" Width="420" WindowStartupLocation="CenterOwner">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Exporting" Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
    <TextBlock Grid.Row="1" Text="{Binding FileName}" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,4,0,8" TextTrimming="CharacterEllipsis" />
    <ProgressBar Grid.Row="2" Style="{StaticResource MaterialDesignLinearProgressBar}" Height="12" Value="{Binding Progress}" Maximum="100" />
    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
      <TextBlock Text="{Binding ProgressText}" Foreground="{DynamicResource MaterialDesignBodyLight}" />
      <TextBlock Text="  " />
      <TextBlock Text="{Binding EtaText}" Foreground="{DynamicResource MaterialDesignBodyLight}" />
    </StackPanel>

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Width="110" Margin="0,0,8,0" Content="Open Folder" Click="OnOpenFolder" IsEnabled="{Binding IsCompleted}" Style="{StaticResource MaterialDesignRaisedButton}" />
      <Button Width="90" Content="Close" Click="OnClose" IsEnabled="{Binding IsCompleted}" Style="{StaticResource MaterialDesignOutlinedButton}" />
    </StackPanel>
  </Grid>
</Window>
