<Window x:Class="GenSubtitle.App.Views.TimeAdjustDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="批量时间调整" Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Mode Selection -->
        <TextBlock Grid.Row="0" Text="调整模式" FontWeight="SemiBold" Margin="0,0,0,12"/>
        <ComboBox Grid.Row="1" x:Name="ModeComboBox" Margin="0,0,0,16">
            <ComboBoxItem Content="全局偏移（秒）" Tag="Offset" IsSelected="True"/>
            <ComboBoxItem Content="按比例缩放（%）" Tag="Scale"/>
        </ComboBox>

        <!-- Value Input -->
        <TextBlock Grid.Row="2" Text="调整值" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBox Grid.Row="2" x:Name="ValueTextBox" materialDesign:HintAssist.Hint="输入数值（正数向前，负数向后）" Margin="0,0,0,8"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" Style="{StaticResource MaterialDesignOutlinedButton}" Width="80" Margin="0,0,8,0" Click="OnCancel"/>
            <Button Content="应用" Style="{StaticResource MaterialDesignRaisedButton}" Width="80" Click="OnApply"/>
        </StackPanel>
    </Grid>
</Window>
